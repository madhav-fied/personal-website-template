import styled from "styled-components";

const styledDiv = styled.div``;

export const StyledProjectCardContainer = styled(styledDiv)`
    height: 25%;
    padding: 8px;
    height: unset;
    border: 2px solid black;

    background-color: rgba(255, 255, 255, 0.5);

    display: flex;
    flex-direction: row;
    gap: 16px;
`;