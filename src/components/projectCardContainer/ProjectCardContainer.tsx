import { StyledProjectCardContainer } from "./StyledElements";

export const ProjectCardContainer = ({children} : any) => {
    return (
        <StyledProjectCardContainer>
            {children}
        </StyledProjectCardContainer>
    )
}